{
  "name": "nutritional-api",
  "version": "1.0.0",
  "description": "API for product nutritional information",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm install --save-dev nodemon"
  },
  "keywords": [
    "nutrition",
    "api",
    "food"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "stripe": "^17.4.0-beta.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
